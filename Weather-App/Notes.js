/*

Create a component where the user will input the city to fine the weather status
    in this case we are using the citySearch components

  in the city search component we create an in put to get the user's search, and then using use state we track the input
   we need to make api called using usestate and this results will be store in an array

   then we use another use state to make activate the activity indicator, in this case it is set to false by default. 

   






















*/